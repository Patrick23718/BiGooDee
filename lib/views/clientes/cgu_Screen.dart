import 'package:flutter/material.dart';

class CGUScreen extends StatelessWidget {
  const CGUScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
