import 'package:flutter/material.dart';

class CoiffeusePrefereScreen extends StatelessWidget {
  const CoiffeusePrefereScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
